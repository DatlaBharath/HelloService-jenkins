pipeline:
  name: build
  identifier: build
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  stages:
    - stage:
        name: Build and Deploy
        identifier: Build_And_Deploy
        type: Custom
        spec:
          execution:
            steps:
              - step:
                  name: Checkout Code
                  identifier: Checkout_Code
                  type: ShellScript
                  spec:
                    shell: Bash
                    source:
                      type: Inline
                      spec:
                        script: |
                          echo "Cloning repo..."
                          rm -rf HelloService-jenkins
                          git clone https://github.com/DatlaBharath/HelloService-jenkins.git
                          cd HelloService-jenkins
                          git checkout main
                          pwd
                          ls
                    executionTarget: {}
                    environmentVariables: []
                    outputVariables: []
                  timeout: 10m
              - step:
                  name: Build Project
                  identifier: Build_Project
                  type: ShellScript
                  spec:
                    shell: Bash
                    onDelegate: true
                    source:
                      type: Inline
                      spec:
                        script: |
                          cd HelloService-jenkins
                          mvn clean install -DskipTests
              - step:
                  name: Build Docker Image
                  identifier: Build_Image
                  type: ShellScript
                  spec:
                    shell: Bash
                    onDelegate: true
                    source:
                      type: Inline
                      spec:
                        script: |
                          cd HelloService-jenkins
                          docker build -t sakthisiddu1/helloservice-jenkins:<+pipeline.sequenceId> .
              - step:
                  name: Push Docker Image
                  identifier: Push_Image
                  type: ShellScript
                  spec:
                    shell: Bash
                    onDelegate: true
                    environmentVariables:
                      - name: DOCKERHUB_USERNAME
                        type: String
                        value: <+secrets.getValue("dockerhub-username")>
                      - name: DOCKERHUB_PASSWORD
                        type: String
                        value: <+secrets.getValue("dockerhub-password")>
                    source:
                      type: Inline
                      spec:
                        script: |
                          cd HelloService-jenkins
                          echo "$DOCKERHUB_USERNAME"
                          echo "$DOCKERHUB_PASSWORD"
                          echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
                          docker push $DOCKERHUB_USERNAME/helloservice-jenkins:<+pipeline.sequenceId>
              - step:
                  name: Deploy to Kubernetes via SSH
                  identifier: Deploy_K8s
                  type: ShellScript
                  spec:
                    shell: Bash
                    onDelegate: true
                    source:
                      type: Inline
                      spec:
                        script: |
                          echo "Creating deployment.yaml and service.yaml..."

                          cat <<EOF > deployment.yaml
                          apiVersion: apps/v1
                          kind: Deployment
                          metadata:
                            name: helloservice-jenkins-deployment
                          spec:
                            replicas: 2
                            selector:
                              matchLabels:
                                app: helloservice-jenkins
                            template:
                              metadata:
                                labels:
                                  app: helloservice-jenkins
                              spec:
                                containers:
                                - name: helloservice-jenkins
                                  image: sakthisiddu1/helloservice-jenkins:<+pipeline.sequenceId>
                                  ports:
                                  - containerPort: 5000
                          EOF

                          cat <<EOF > service.yaml
                          apiVersion: v1
                          kind: Service
                          metadata:
                            name: helloservice-jenkins-service
                          spec:
                            type: NodePort
                            selector:
                              app: helloservice-jenkins
                            ports:
                              - port: 5000
                                targetPort: 5000
                                nodePort: 30007
                          EOF

                          echo "Deploying to Kubernetes via SSH..."

                          ssh -o StrictHostKeyChecking=no -i /home/harness/.ssh/test.pem ubuntu@65.0.29.174 "kubectl apply -f -" < deployment.yaml
                          ssh -o StrictHostKeyChecking=no -i /home/harness/.ssh/test.pem ubuntu@65.0.29.174 "kubectl apply -f -" < service.yaml

                          rm -rf HelloService-jenkins